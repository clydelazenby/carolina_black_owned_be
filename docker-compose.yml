version: '3.1'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Dipset07!  # Add this line
      'ENGINE': 'django.db.backends.mysql'
      'NAME': 'carolinablackowned'
      'USER': 'clazenby'
      'PASSWORD': 'Dipset07!'
      'HOST': 'localhost'
      'PORT': '3306'

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080